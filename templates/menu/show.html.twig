{% extends 'base.html.twig' %}

{% block title %}Menu
{% endblock %}

{% block body %}
	<h1>Menu</h1>

	<table class="table">
		<tbody>
			<tr>
				<th>Id</th>
				<td>{{ menu.id }}</td>
			</tr>
			<tr>
				<th>DetailsMenu</th>
				<td>{{ menu.detailsMenu }}</td>
			</tr>
		</tbody>
	</table>
	<div class="container">

		<h1>Créer un Nouveau Plats</h1>
		{{ include('plats/_form.html.twig') }}

		<a href="{{ path('plats_index') }}">Liste des plats</a>

	</div>
	<a href="{{ path('menu_index') }}">Liste des menus</a>

	<a href="{{ path('menu_edit', {'id': menu.id}) }}">Modifier</a>

	{{ include('menu/_delete_form.html.twig') }}
{% endblock %}
