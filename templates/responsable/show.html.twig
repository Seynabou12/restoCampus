{% extends 'base.html.twig' %}

{% block title %}Restaurant{% endblock %}

{% block body %} 
<section>
<div class="container-fluid" style="position:absolute; color:white;margin-top:30px">
    <h1>Restaurant</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Nom</th>
                <td>{{ responsable.nom }}</td>
            </tr>
            <tr>
                <th>Lieu</th>
                <td>{{ responsable.prenom }}</td>
            </tr>
            <tr>
                <th>Lieu</th>
                <td>{{ responsable.telephone }}</td>
            </tr>
            <tr>
                <th>Lieu</th>
                <td>{{ responsable.adresse }}</td>
            </tr>
        </tbody>
    </table>

       <div class="container-fluid">
            <h1>Ajouter de Resto</h1>
            {{ include('restaurant/_form.html.twig') }}
            <a href="{{ path('restaurant_index') }}">back to list</a>
      </div> 
    <a href="{{ path('responsable_index') }}">back to list</a>

    <a href="{{ path('responsable_edit', {'id': responsable.id}) }}">edit</a>

    {{ include('responsable/_delete_form.html.twig') }}
    </div>
    </section>
{% endblock %}